{% extends 'base.html.twig' %}

{% block title %}RecipeIngredient{% endblock %}

{% block body %}
    <h1>RecipeIngredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_ingredient.id }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ recipe_ingredient.qte }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recipe_ingredient_index') }}">back to list</a>

    <a href="{{ path('recipe_ingredient_edit', {'id': recipe_ingredient.id}) }}">edit</a>

    {{ include('recipe_ingredient/_delete_form.html.twig') }}
{% endblock %}
