{% extends 'base.html.twig' %}

{% block title %}{{ user.username }}{% endblock %}

{% block body %}
    <div class="user-rens p-5 mt-5 d-flex flex-column">
        <div class="d-flex justify-content-between align-items-center">
            <img src="{{ asset('images/Avatar.png') }}" alt="">
            <div class="ml-5">
                <p class="h3">{{ user.username }}</p>
                <p class="h3">{{ user.email }}</p>
            </div>
            <a class="add-recipe btn btn-success ml-5" href="{{ path('recipe_new') }}">Ajouter une recette</a>
        </div>
        <a class="btn btn-success col-2 offset-5 mt-5" href="{{ path('menu_index') }}">Mes recettes favorites</a>
        <p>Derniers menus validés</p>
        <p>Dernière liste de courses</p>
        <p>Menus les plus commandés</p>
        <p>Récapitulatif de mes préférences</p>


    </div>




    <a href="{{ path('user_index') }}">back to list</a>

    <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
