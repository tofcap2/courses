{% extends "base.html.twig" %}

{% block body %}

    {{ recipe[0].title }}
    {{ recipe[0].category.label }}
    <img src="{{ recipe[0].{% asset('uploads/' ~ picture.filename) %} }}" alt="">
{% endblock %}
